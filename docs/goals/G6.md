# G6 — Likeness UI

**Status:** open
**Branch:** `feat/likeness-ui`
**Wave:** 1 · **Kind:** web · **Depends on:** W0 · **Migrations:** —

## Why

This is where the phase's second exit is seen: upload a part that is already here, and the app says so. Design:
[`phase-6.md`](phase-6.md).

## Facts checked (re-check against W0's merge)

- W0's bindings: `Likeness`, `DuplicateClusters`, `SetLink`, `FoldPart`, `PartCard`; the `likeness` block in
  `strings.ts`; `titles.duplicates`.
- The backend is G3, built in parallel. Develop against the bindings with `fetch` mocked, as every route test here
  does; the live check waits for G3's merge.

## Rules

- Owns: `web/src/lib/likeness.ts`; `web/src/routes/duplicates.tsx` and its test (`/duplicates?library=&since=`);
  `web/src/components/Likeness.tsx`; its mount point in `PartDetail.tsx`; "folded into *X*" on `removed.tsx`; in
  `index.tsx` only the batch-finished line ("N look like parts already here: Review", linking `?since=` to the batch's
  start); the `likeness` strings block.
- Every string in the `likeness` block; dark only; motion through `web/src/lib/motion.ts`; every route titled, every
  list with a role; `cargo xtask verify fast` and `npm --prefix web test` / `run build` while developing — no Rust
  builds needed.

## Stages

1. **Part page** — sections Identical, Near-duplicates (with **Fold into this**, **Link as variant**, **Not the same**),
   Variants, More like this; "not profiled yet" when `profiled` is false. The fold dialog says nothing is deleted and
   nothing moves, and Restore undoes it.
2. **Review queue** — `/duplicates`: clusters, newest first, the same three actions; empty state that says why.
3. **Removed page** — a folded part says where it went. **Upload/scan** — the finished line offers a review.
4. **Tests:** each section and action with `fetch` mocked; the dialog's words; a fold then the Removed page; the
   route title and list roles; no bare strings.

## Done when

Web tests green; after G3 merges, the lead runs the phase exit (below) on the merged tree. Hand off when the web side
is done; the lead merges after G3.

**Phase exit 2 (measured by the lead after merge):** upload a rotated copy of `fixtures/bracket-lp-1042-03.stl` into a
library holding the original — the finished line offers a review, the part page lists the original as a
near-duplicate, Fold then Restore round-trips.

## Record

(the lane writes)
