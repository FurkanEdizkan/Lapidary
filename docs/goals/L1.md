# L1 — Sharing screens, seen and finished

**Status:** open
**Branch:** `fix/sharing-screens`
**Wave:** 1 · **Kind:** web (+ one db read) · **Depends on:** B0 · **Migrations:** —

## Why

Goal 9 shipped a dozen new sharing screens with component tests and nobody has looked at them rendered: the member
picker, the members line and its dialog, the introductions card, the People list's "introduced by" and "no folder in
common", the shared folder page's "as *X* read it", the seeding switch and its count, Download / "In your library", and
the queue position. Two owner-side gaps recorded in the ROADMAP ride along.

## Facts checked (as of `3964524`)

- The screens: `web/src/routes/sharing.tsx`, `web/src/routes/sharing_.shares.$shareId.tsx`,
  `web/src/components/ShareDialog.tsx`.
- ROADMAP (Shared libraries, S4 "left for later"): switching an existing share to ask first is api-only — the page
  offers it only when sharing; a share switched back to open still lists its old requests, because
  `PgShares::requests` (`crates/lapidary-db/src/shares.rs` ~458) reads every grant row as though its share asked first.
- Screenshots on this machine: headless `google-chrome`, one profile per shot, CDP for timing (see the Record of the
  web UI refinement goal in ROADMAP). A native stack runs from `target/dev-stack/stack.sh`; goal 7 ran two native
  installations under `target/sharing-check/`.

## Rules

- Owns: `web/src/routes/sharing.tsx`, `web/src/routes/sharing_.shares.$shareId.tsx`,
  `web/src/components/ShareDialog.tsx`, the `sharing` strings block, `PgShares::requests`.
- Three installations run **natively** on the lane's port block, their databases created on the lane's own test
  database server — no Docker builds. Never ports 3000/8080, never the owner's browser profile.
- Fix what the review finds within the files above; anything outside them goes in the Record as left for later.

## Stages

1. **Before** — the three installations, the goal-9 scenario (pair, share with members, introductions, pull, relay,
   seeding, one-part download, take back), captures of every screen at desktop (1440) and phone (390) widths.
2. **Review** — the impeccable detector on the three files, then the `impeccable-finish-reviewer` agent on the
   captures; fix everything it returns that is inside this goal's files; recapture.
3. **Owner-side gaps** — the sharing page can switch an existing share to ask first (and back); a share that no longer
   asks first stops listing its old requests. **Tests:** web tests for the switch; a db test that switching back to
   open drops the requests and switching to ask again brings them back.
4. Mutation check: the mode filter removed from `requests`.

## Done when

Before/after captures and the reviewer's list closed in the Record, `verify slice` green, handed off.

## Record

(the lane writes)
