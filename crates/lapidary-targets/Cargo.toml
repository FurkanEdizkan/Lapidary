[package]
name = "lapidary-targets"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
lapidary-core.workspace = true
# The bundle writer's per-entry CRC, which a STORE entry's data descriptor carries.
crc32fast.workspace = true
thiserror.workspace = true
serde.workspace = true

[dev-dependencies]
# Reads a written bundle back, as any other tool would.
zip.workspace = true

[lints]
workspace = true
